slug = "symphony-canary"
repository = "duck-lint/symphony-canary"
git_remote = "git@github-symphony-canary:duck-lint/symphony-canary.git"
secret_reference = "github.token"
dispatch_labels = ["symphony:auto"]
blocked_label = "symphony:human"
dashboard_port = 4041
max_concurrent_agents = 1
max_turns = 8
poll_interval_ms = 5000
max_retry_backoff_ms = 300000
codex_model = "gpt-5.6-luna"
codex_reasoning_effort = "high"
toolchain = "python"
prevent_host_sleep = true
notifications_enabled = true
display_name = "symphony-canary"
notification_backend = "windows-toast"
